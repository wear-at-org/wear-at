spring:
  config:
    activate:
      on-profile: "local"
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    url: "jdbc:mysql://localhost:3306/scot?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC"
    username: "scot"
    password: "scot123!@#"
  jpa:
    properties:
      hibernate:
        format_sql: true
        ddl-auto: create
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
  properties:
    name: "local"
    age:
      - 20
      - 21
      - 22
